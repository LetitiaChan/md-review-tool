# 批阅记录

- **源文件**：project-continuity.mdc
- **源文件路径**：f:/github/md-review-tool/.codebuddy/rules/project-continuity.mdc
- **源文件版本**：未知
- **批阅时间**：20260422_1837
- **批阅版本**：v2
- **批注数量**：0
  - 评论：0
  - 删除：0
  - 后插：0
  - 前插：0

---

## 操作指令

> 指令已按**从后往前**排列（倒序），请严格按照顺序从上到下逐条执行。
> 每条指令提供了「文本锚点」用于精确定位，请优先通过锚点文本匹配来确认目标位置，blockIndex 仅作辅助参考。

---

## 原始数据（JSON）

> 如需精确操作，可使用以下 JSON 数据。其中 `blockIndex` 是基于空行分割的块索引（从0开始），`startOffset` 是目标文本在块内的字符偏移量（从0开始），可用于区分同一块内的重复文本。

```json
{
  "fileName": "project-continuity.mdc",
  "docVersion": "未知",
  "reviewVersion": 2,
  "annotationCount": 0,
  "rawMarkdown": "---\r\ndescription: 每次会话开始时必须执行\r\nalwaysApply: true\r\ntype: always\r\nenabled: true\r\n---\r\n\r\n> 🤖 读取本文件时输出：`📖 .aikp/project-continuity`\r\n\r\n# Project Continuity — 跨会话上下文恢复 + 开发纪律\r\n\r\n## 一、会话启动：读取进度笔记\r\n\r\n读取仓库根目录下的 `AGENT-PROGRESS.md` 文件。该文件记录了：\r\n\r\n- 项目概要（技术栈、定位、构建工具）\r\n- 已完成的变更（已归档的 OpenSpec changes）\r\n- 待实施的变更（活跃 changes 及其优先级）\r\n- 已积累的规格（capability specs）\r\n- 项目目录结构要点\r\n- 已知问题与注意事项\r\n- 下次会话建议\r\n\r\n### 快速定向\r\n\r\n在心中建立以下上下文（不需要冗长地播报，但需要内化理解）：\r\n\r\n- 上次做到哪里了？\r\n- 当前项目处于什么阶段？\r\n- 有什么已知的坑或注意事项？\r\n\r\n### 环境确认（每次会话启动时执行）\r\n\r\n```bash\r\ngit --no-pager log --oneline -5\r\n```\r\n\r\n- 确认最近提交与 AGENT-PROGRESS.md 记录一致\r\n- 如果发现未推送的本地 commit → 提醒用户\r\n- 如果发现 AGENT-PROGRESS.md 中未记录的活跃 change → 自动补充记录\r\n\r\n---\r\n\r\n## 二、开发纪律：强制 OpenSpec 工作流（最重要）\r\n\r\n> **核心原则：本项目采用 OpenSpec spec-driven 工作流。任何功能开发和重大修改都必须通过 OpenSpec 流程，绝不允许跳过 spec 直接写代码。**\r\n\r\n### 规则 1：用户请求新功能或重大修改时\r\n\r\n当用户说出类似以下内容时，**必须走 OpenSpec 流程**：\r\n- \"帮我实现 XXX 功能\"\r\n- \"做下一个功能\"\r\n- \"加一个 XXX\"\r\n- 任何涉及新增页面、新增 API/接口、新增数据表、新增服务模块的需求\r\n\r\n**执行流程：**\r\n\r\n```\r\n1. 检查 openspec/changes/ 是否已有对应 change\r\n   ├── 有，且已有 tasks.md → 直接调用 openspec-apply-change 技能\r\n   ├── 有，但缺少 artifacts → 先调用 openspec-continue-change 补齐，再 apply\r\n   └── 没有 → 先调用 openspec-propose 创建完整提案（proposal + design + tasks），再 apply\r\n2. 绝对不要跳过上述流程直接写代码\r\n```\r\n\r\n### 规则 2：小修/热修例外（Hotfix Exemption）+ Hotfix Mini-Pipeline\r\n\r\n以下情况**可以不走 OpenSpec**，直接修改代码：\r\n- 修复 typo、拼写错误\r\n- 修改 1-2 行的明显 bug\r\n- 纯样式/颜色/间距微调\r\n- 修改配置文件或注释\r\n- 纯文档变更（README、规则文件、进度笔记、策划文档等非代码文件）\r\n\r\n**判定标准：源码文件 ≤3 个 且 源码改动 ≤30 行**。超过此阈值必须走 OpenSpec。\r\n（注：测试文件的新增/修改不计入阈值统计，因为 H3.3 测试补全可能产生额外的测试代码）\r\n\r\n**⚠️ 即使是 Hotfix，也必须执行 Hotfix Mini-Pipeline（不可跳过）：**\r\n\r\n````\r\nHotfix Mini-Pipeline（强制，修改后立即执行）:\r\n\r\nH0. 变更类型分流（首先执行）\r\n    - 判断本次修改是否涉及代码文件（src/、test/、webview/、package.json 等）\r\n    - 如果 **不涉及任何代码文件**（仅修改了文档、规则文件、进度笔记、策划文档等）：\r\n      → 走 Doc-Only 快速通道：直接跳到 H5 Git Commit → H6 Git Push → H7 报告\r\n      → 跳过 H1~H4（构建、测试、打包均不需要）\r\n      → commit message 前缀使用 `docs:` 或 `chore:`\r\n    - 如果 **涉及代码文件但属于纯重构**（不改变外部行为，如重命名、提取函数、调整结构）：\r\n      → 走 Refactor 通道：执行 H1→H2→H3（构建+回归测试验证不破坏现有行为），跳过 H3.3（不需要新增测试）\r\n      → 继续 H4→H5→H6→H7\r\n      → commit message 前缀使用 `refactor:`\r\n    - 如果 **涉及代码文件且属于 bugfix / 功能微调** → 继续执行完整的 H1~H7 流程\r\n\r\nH1. 影响面分析\r\n    - 列出修改的文件及改动行数\r\n    - 分析直接影响（被修改的模块）和间接影响（依赖该模块的上下游）\r\n    - 判断不受影响的模块（为回归测试提供对照）\r\n\r\nH2. 构建验证\r\n    cd f:/github/md-review-tool && npm run compile\r\n    - 失败 → 修复编译错误，最多 3 次重试\r\n    - 成功 → 继续\r\n\r\nH3. 回归测试\r\n    cd f:/github/md-review-tool && npm test\r\n    - 全部通过 → 继续\r\n    - 有失败 → 进入 Auto-Fix Loop（最多 3 轮）\r\n    - 仍失败 → STOP，报告给用户\r\n\r\nH3.3. 测试用例补全（强制，不可跳过，含三层测试模型）\r\n    - 遵循三层测试模型（与全管线 Step 9 一致）：\r\n      · **Tier 1 — 存在性断言**：API 暴露检查、DOM 元素存在性检查、源码关键字断言\r\n      · **Tier 2 — 行为级断言**：模拟真实用户操作（拖拽、点击、键盘等），验证 DOM 状态实际变化\r\n      · **Tier 3 — 任务特定断言**：针对本次修复的具体场景，至少 1 个命名为 `BT-<模块>.<序号> <描述>` 的断言\r\n    - 检查本次 bugfix 修改的模块/功能是否已有对应的测试用例\r\n    - 如果没有 → 必须新增针对该 bug 修复的回归测试用例（覆盖 Tier 1~3）\r\n    - 如果已有 → 检查是否覆盖了本次修复的具体场景，不足则追加\r\n    - **同步检查 UI 交互覆盖**：如果修复涉及 UI 交互行为，\r\n      必须在同一轮中一并补全行为级断言（Tier 2），不要拆成单独步骤\r\n    - 所有测试用例（Tier 1 + Tier 2 + Tier 3）补全后，统一执行一次 npm run compile && npm test 确认通过\r\n    - 目标：确保该 bug 一旦回归能被自动测试发现，且 UI 交互行为的回归也能被捕获\r\n    - ⚠️ 禁止先补回归测试跑一轮、再补行为级测试又跑一轮 —— 必须一次性补全、一次性验证\r\n    - ⚠️ 如果 H3 已全部通过，H3.3 补全新测试后可只运行新增测试验证（`npm test -- --grep \"新增测试名\"`），\r\n      无需重跑全量回归（全量回归已在 H3 中完成）\r\n\r\nH3.5. 运行时 Bug 诊断策略（复杂 Bug 专用，按需执行）\r\n    - **触发条件**：Bug 只在运行时出现，自动测试环境无法直接复现（如依赖真实 VS Code 宿主环境、\r\n      用户交互时序、特定文件内容等）\r\n    - **策略 A — 诊断日志 + 自动测试捕获**：\r\n      · 在可疑代码路径中插入诊断日志（console.log / OutputChannel），标记 `[DIAG]` 前缀\r\n      · 编写测试用例，通过 mock/stub 模拟运行时条件，捕获日志输出并断言关键信息\r\n      · 示例：`assert(logOutput.includes('[DIAG] 实际收到的值: ...'))` \r\n      · 诊断日志在 Bug 确认修复后应清理或降级为 debug 级别\r\n    - **策略 B — 用户协助验证**：\r\n      · 当策略 A 无法覆盖（如必须在真实扩展宿主中运行），在修复代码中保留诊断日志\r\n      · 完成 H4 打包后，明确告知用户：\r\n        \"本次修复涉及运行时行为，已插入诊断日志，请安装新包后复现操作，\r\n         并反馈 Output Channel / DevTools Console 中 [DIAG] 开头的日志输出\"\r\n      · 根据用户反馈的日志判断修复是否生效，必要时进入第二轮修复\r\n    - **策略选择**：优先尝试策略 A（自动化）；仅当自动化确实无法模拟运行时条件时才降级到策略 B\r\n    - **⚠️ 无论使用哪种策略，都必须在 H7 报告中注明诊断方式和结论**\r\n\r\nH4. 打包发布（测试通过后、commit 前）\r\n    cd f:/github/md-review-tool && npx vsce package --no-dependencies\r\n    - 成功 → 安装包已生成\r\n    - 失败 → 报告错误但不阻塞管线（打包失败不影响代码质量）\r\n\r\nH5. Git Commit\r\n    git add -A && git commit -m \"fix: <简述>\"\r\n\r\nH6. Git Push（自动推送）\r\n    git push\r\n    - 推送失败 → 报告错误，等待用户处理\r\n\r\nH7. Hotfix 报告\r\n    输出 Hotfix 专属结果表：\r\n\r\n    ``` markdown\r\n    ## ✅ Hotfix Complete: <简述>\r\n\r\n    | Step          | Status | Details                         |\r\n    |---------------|--------|---------------------------------|\r\n    | 分流          | ✅     | 代码变更 / Doc-Only / Refactor  |\r\n    | 影响面分析    | ✅     | N 文件, 风险: 低/中/高          |\r\n    | 构建          | ✅     | 编译通过                        |\r\n    | 回归测试      | ✅     | N/N 通过                        |\r\n    | 测试补全      | ✅     | 新增 N 个用例 (Tier 1/2/3)      |\r\n    | 诊断策略      | ⏭️     | 不适用 / 策略A / 策略B          |\r\n    | 打包          | ✅     | .vsix 已生成                    |\r\n    | Git Commit    | ✅     | <hash> fix: <描述>              |\r\n    | Git Push      | ✅     | pushed to origin/main           |\r\n    ```\r\n\r\n    （Doc-Only 通道只显示：分流 → Git Commit → Git Push 三行）\r\n    （Refactor 通道不显示：测试补全、诊断策略 两行）\r\n````\r\n\r\n**Hotfix Mini-Pipeline 与全管线的区别：**\r\n- 无 OpenSpec verify / archive / spec-sync / AGENT-PROGRESS 更新\r\n- 有完整的 构建 → 影响分析 → 回归测试 → 打包 → commit → push\r\n\r\n**连续 Hotfix 合并策略：**\r\n如果用户在同一会话中连续提出多个 Hotfix 级别的修改：\r\n- 可以先累积修改，最后统一走一次 H0→H7 管线\r\n- 判定标准：累积改动仍在 Hotfix 阈值内（源码文件 ≤3 个 且 源码改动 ≤30 行）\r\n- 如果累积超过阈值 → 转为 OpenSpec 流程\r\n- Agent 应主动提示用户：\r\n\r\n### 规则 3：优先级意识\r\n\r\n实现功能前，对照 `AGENT-PROGRESS.md` 中的优先级建议：\r\n- 如果用户请求的功能**与建议一致** → 直接执行\r\n- 如果用户请求的功能**与建议不同** → 简短提醒（\"AGENT-PROGRESS.md 建议下一步做 X，你确定要先做 Y 吗？\"），但**服从用户决定**\r\n\r\n### 规则 4：禁止行为清单\r\n\r\n- ❌ 收到\"帮我实现 X\"后直接开始写代码\r\n- ❌ 不看 changes 目录就创建重复的 change\r\n- ❌ 跳过 proposal/design 直接写 tasks\r\n- ❌ 实现完成后不跑测试就宣布完成\r\n- ❌ 手动执行 archive 时遗漏 spec sync\r\n- ❌ 跳过测试步骤（构建→测试→打包是强制步骤，不可跳过）\r\n- ❌ 未归档未 commit 就开始下一个变更的 propose 或 apply\r\n- ❌ 流水线执行到一半就开始新工作（必须完整走完全管线或显式刹车报告）\r\n- ❌ Git commit message 描述部分使用英文\r\n- ❌ 对同一文件在同一轮中发起多次并行 replace_in_file（会因文件内容已变导致后续匹配失败，必须串行执行）\r\n- ❌ 测试只写存在性断言（Tier 1）而不写行为级断言（Tier 2）—— 必须为每个涉及 UI 交互的功能编写模拟真实操作的行为级测试\r\n- ❌ 不为当前变更的新功能编写特定测试用例（Tier 3）—— 每次 apply 和 hotfix 都必须有针对性的新测试\r\n- ❌ 功能增删改后不更新相关文档（README、帮助说明等）—— 任何用户可感知的功能变更都必须同步更新文档（参见规则 6）\r\n\r\n### 规则 4.5：阶段完成自检（防止提前收尾）\r\n\r\n> **核心原则：每完成一个阶段（propose / apply / build / test / verify / archive / commit），立即自检是否还有后续步骤，是否达到了用户定义的\"完成\"。**\r\n\r\n**自检流程（每个阶段结束后强制执行）：**\r\n\r\n```\r\n1. 回溯用户的原始请求，确认\"完成\"的边界\r\n   - 用户说\"完成 X\" → 必须走完 propose → apply → build → test → verify → archive → commit 全管线\r\n   - 用户说\"完成 X & Y\" → X 和 Y 都必须走完全管线才算完成\r\n   - 用户说\"propose X\" → 只需 propose 即可\r\n2. 对照当前进度，判断是否已达到用户定义的完成边界\r\n3. 如果未达到 → 立即继续下一个步骤，不输出总结报告，不暗示用户需要手动触发\r\n4. 如果已达到 → 输出最终管线报告 + 更新 AGENT-PROGRESS.md\r\n```\r\n\r\n**禁止行为：**\r\n- ❌ 在管线中途（如 propose 完成后）输出\"会话总结\"式的格式化报告\r\n- ❌ 用\"下一步: 运行 /opsx:apply\"之类的措辞把控制权交还给用户（除非用户明确要求分步确认）\r\n- ❌ 因为上下文已经很长就产生\"该收尾了\"的倾向——管线完整性优先于上下文压力\r\n\r\n### 规则 5：完成门禁 — 新工作的前置检查（最关键）\r\n\r\n> **核心原则：在开始任何新的 propose 或 apply 之前，必须确认不存在\"已实施但未归档\"的变更。**\r\n\r\n**每次用户请求新功能、propose、或 apply 时，执行以下检查：**\r\n\r\n```\r\n1. 扫描 openspec/changes/ 下所有非 archive 目录\r\n2. 对每个变更，检查是否存在 tasks.md\r\n3. 如果 tasks.md 存在，检查是否所有任务都已 [x] 完成\r\n4. 如果发现「全部完成但未归档」的变更：\r\n   ├── ⛔ 立即停止，不允许开始新工作\r\n   ├── 报告：\"变更 <name> 已实施完成但尚未归档\"\r\n   ├── 自动执行该变更的流水线后半段（build → test → archive → commit）\r\n   └── 完成后再继续用户请求的新工作\r\n5. 如果没有未归档的已完成变更 → 正常继续\r\n6. 附加检查：如果发现活跃 change 未记录在 AGENT-PROGRESS.md 的\"待实施的变更\"表格中\r\n   → 自动补充记录（变更名 + 描述 + 当前状态），确保进度笔记与实际 changes 目录一致\r\n```\r\n\r\n**这条规则没有例外，不接受\"先做新的回头再归档\"。**\r\n\r\n### 规则 6：文档同步纪律（功能增删改必须更新文档）\r\n\r\n> **核心原则：任何用户可感知的功能变更（新增、修改、删除），都必须同步更新相关文档。代码改了但文档没改 = 未完成。**\r\n\r\n**触发条件（满足任一即需更新文档）：**\r\n- 新增了用户可使用的功能、命令、配置项\r\n- 修改了现有功能的行为、参数、交互方式\r\n- 删除或废弃了某个功能\r\n- 修改了安装方式、依赖要求或使用前提\r\n\r\n**必须检查并更新的文档：**\r\n- `README.md` — 功能列表、使用说明、配置说明等相关章节\r\n- 帮助说明文档（如 `docs/` 下的用户指南、策划文档中的功能描述）\r\n- `package.json` 中的 `contributes` 描述（如涉及 VS Code 命令/配置变更）\r\n\r\n**执行时机：**\r\n- **OpenSpec 全管线**：在测试验证全部通过后（Step 9 之后）、打包之前（Step 9.5 之前），检查并更新文档\r\n- **Hotfix Mini-Pipeline**：在测试验证全部通过后（H3.3 之后）、打包之前（H4 之前），检查并更新文档\r\n\r\n**判定标准：**\r\n- 纯内部重构（不改变外部行为）→ 无需更新文档\r\n- Bug 修复（修复的是文档中已描述的预期行为）→ 无需更新文档\r\n- Bug 修复（修复后行为与文档描述不一致）→ 需要更新文档\r\n- 新功能 / 功能变更 / 功能删除 → **必须更新文档**\r\n\r\n---\r\n\r\n## 三、会话结束：更新进度笔记\r\n\r\n当一次重要的开发/实施会话结束时（例如完成了 OpenSpec apply、propose、或重大代码修改），**主动** 更新 `AGENT-PROGRESS.md`：\r\n\r\n- 在 \"最近更新\" 部分添加当日记录\r\n- 更新变更状态（如有完成的 change）\r\n- 更新 \"已知问题\" 和 \"下次会话建议\"\r\n\r\n---\r\n\r\n## 项目关键文件索引\r\n\r\n<!-- ⚠️ 请替换为你的项目实际文件 -->\r\n| 文件 | 用途 |\r\n|------|------|\r\n| `AGENT-PROGRESS.md` | 跨会话记忆（最重要） |\r\n| `docs/[策划文档].md` | 产品策划文档 |\r\n| `openspec/changes/` | OpenSpec 变更管理 |\r\n| `openspec/specs/` | 能力规格库 |\r\n| `src/` | 应用代码根目录 |\r\n| `CODEBUDDY.md` | CodeBuddy Code 的项目入口文件（仅保留项目本体说明 + 工作流摘要；流程规则真源在本文件） |\r\n| `.aikp/rules/` | 流程规则**真源**（本目录由 `npm run sync-aikit` 同步到 `.codebuddy/`、`.claude/`、`.cursor/`） |\r\n\r\n### AI 工具入口分工（重要）\r\n\r\n- **真源唯一**：所有流程规则（本文件、未来新增的 `.aikp/rules/*.mdc`）的真源统一在 `.aikp/rules/`。\r\n- **CODEBUDDY.md**：CBC 的原生入口，保留\"项目概述 / 常用命令 / 架构 / 关键约束\"等项目本体说明；流程规则部分只保留一段摘要 + 指向 `.aikp/rules/` 的引用块（区块标记 `<!-- AIKP-RULES:START -->` / `<!-- AIKP-RULES:END -->`）。CBC 读到该文件时应按指引**立即读取**引用块中列出的真源文件。\r\n- **Shim 目录**：`.codebuddy/rules/`、`.claude/rules/`、`.cursor/rules/` 下的同名 `.mdc` 文件均为 shim，自动由 `scripts/sync-aikit-shims.js` 生成，指向同一 `.aikp/` 真源。\r\n- **一致性校验**：`npm run sync-aikit:check` 同时检查 shim 同步状态 **与** `CODEBUDDY.md` 引用块清单是否与 `.aikp/rules/` 实际文件一致。如果新增/删除/改名 `.aikp/rules/*.mdc`，必须同步更新 `CODEBUDDY.md` 的引用块。\r\n\r\n## 开发环境\r\n\r\n- **操作系统**: Windows\r\n- **Shell**: PowerShell (Core)\r\n- **技术栈**: TypeScript + VS Code Extension API + Mocha + marked\r\n- **构建工具**: tsc (TypeScript Compiler)，npm scripts\r\n- **工作流**: OpenSpec spec-driven，通过 `/opsx:*` 命令驱动\r\n\r\n## 发布规则（双市场发布）\r\n\r\n> **核心原则：每次用户说\"发布\"时，必须同时发布到 VSCode Marketplace 和 Open VSX Registry（Cursor 市场），缺一不可。**\r\n\r\n**发布流程：**\r\n\r\n```\r\n1. 打包 .vsix 文件\r\n   npx vsce package --no-dependencies\r\n\r\n2. 发布到 VSCode Marketplace\r\n   npx vsce publish --no-dependencies\r\n\r\n3. 发布到 Open VSX Registry（Cursor 市场）\r\n   npx ovsx publish <生成的 .vsix 文件名> -p <OVSX_TOKEN>\r\n   （Token 通过环境变量 OVSX_TOKEN 获取，如未设置则提示用户提供）\r\n\r\n4. 发布完成后，输出两个市场的安装链接：\r\n   - VSCode Marketplace: https://marketplace.visualstudio.com/items?itemName=letitia.md-human-review\r\n   - Open VSX (Cursor): https://open-vsx.org/extension/letitia/md-human-review\r\n```\r\n\r\n**注意事项：**\r\n- 两个市场都发布成功才算\"发布完成\"\r\n- 如果其中一个失败，必须报告失败原因并尝试修复\r\n- 发布前确保已通过构建和测试\r\n\r\n---\r\n\r\n## 命令执行策略\r\n\r\n> **所有命令直接执行，无需确认。** 包括但不限于：构建、测试、打包、Git commit/push、安装依赖等操作，一律自动执行，不逐步询问用户确认。\r\n\r\n## Git 规范\r\n\r\n<!-- ⚠️ 根据团队习惯配置 commit message 语言 -->\r\n- **Commit message 格式**: `feat: 描述`\r\n- 类型前缀：`feat:` / `fix:` / `refactor:` / `chore:`\r\n\r\n## 设计理念\r\n\r\n本规则的设计灵感来自 Anthropic \"Effective Harnesses for Long-Running Agents\"，目的是解决 AI Agent 在多次会话之间的 8 个核心失败模式：\r\n\r\n1. **One-shot tendency** → 通过读取 AGENT-PROGRESS.md 恢复上下文\r\n2. **Premature completion claims** → 通过 OpenSpec task list 追踪真实进度\r\n3. **Lack of cross-session memory** → 通过文件系统持久化进度笔记\r\n4. **Messy environment** → 通过构建检查和环境初始化\r\n5. **Skipping structured workflow** → 通过开发纪律规则强制走 OpenSpec 流程\r\n6. **Incomplete pipeline execution** → 通过完成门禁（规则 5）阻止在未归档状态下开始新工作\r\n7. **Premature stage exit** → 通过阶段完成自检（规则 4.5）强制回溯用户原始请求边界\r\n8. **Unverified hotfix** → 通过 Hotfix Mini-Pipeline（规则 2）确保即使是小修也经过构建→影响分析→回归测试→打包→commit→push 全链路验证\r\n",
  "annotations": []
}
```